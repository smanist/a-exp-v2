{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "outcome",
    "next_state",
    "blocker_kind",
    "summary",
    "experiments",
    "verification",
    "files_changed",
    "artifacts",
    "next_direction",
    "open_questions",
    "budget_used"
  ],
  "properties": {
    "outcome": {
      "type": "string",
      "enum": ["progress", "needs_human", "paused", "blocked", "completed"]
    },
    "next_state": {
      "type": "string",
      "enum": ["ready", "needs_human", "paused", "blocked", "completed"]
    },
    "blocker_kind": {
      "type": ["string", "null"],
      "enum": [
        null,
        "scientific_decision",
        "approval_required",
        "external_resource_unavailable"
      ],
      "description": "Human-owned blocker classification; null unless next_state is needs_human. Runner-owned Git closeout and transient infrastructure failures do not qualify."
    },
    "summary": {"type": "string", "minLength": 1},
    "experiments": {"type": "array", "items": {"type": "string"}},
    "verification": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["command", "result"],
        "properties": {
          "command": {"type": "string", "minLength": 1},
          "result": {"type": "string", "minLength": 1}
        }
      }
    },
    "files_changed": {"type": "array", "items": {"type": "string"}},
    "artifacts": {"type": "array", "items": {"type": "string"}},
    "next_direction": {"type": ["string", "null"]},
    "open_questions": {"type": "array", "items": {"type": "string"}},
    "budget_used": {
      "type": "object",
      "additionalProperties": false,
      "required": ["wall_seconds", "experiments"],
      "properties": {
        "wall_seconds": {"type": "number", "minimum": 0},
        "experiments": {"type": "integer", "minimum": 0}
      }
    }
  }
}

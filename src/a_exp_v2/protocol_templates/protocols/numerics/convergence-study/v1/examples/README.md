# Examples

Example experiment instances for `numerics.convergence-study.v1` should be
stored here when they are useful as reusable references.

Keep examples method and problem specific, but keep the protocol itself generic.
An example may demonstrate a particular PDE, geometry, model class, solver,
sample-size study, tolerance study, or method comparison.

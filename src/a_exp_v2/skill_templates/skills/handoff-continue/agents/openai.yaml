interface:
  display_name: "Handoff Continue"
  short_description: "Resume GPU work under an unchanged study goal"
  default_prompt: "Use $handoff-continue to record interactive evidence and return this unchanged goal to autonomous GPU work."
policy:
  allow_implicit_invocation: false
